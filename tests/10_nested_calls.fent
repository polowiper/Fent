define add(const a, const b) {
    return a + b;
}

define multiply(const a, const b) {
    return a * b;
}

define compute(const x, const y, const z) {
    const sum = add(x, y);
    const prod = multiply(sum, z);
    return prod;
}

const a = 5;
const b = 10;
const c = 2;

const result = compute(a, b, c);
const nested = add(multiply(a, b), multiply(b, c));
const deep = add(add(a, b), add(c, a));

return result;
