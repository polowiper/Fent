const x = 10;

{
    const y = 20;
    const z = x + y;
}

{
    const a = 1;
    {
        const b = 2;
        {
            const c = 3;
            const sum = a + b + c;
        }
    }
}

if (x > 5) {
    {
        const inner = 42;
    }
    const outer = 99;
}

return x;
