var a = 10;
var b = 3;

var sum = a + b;
var diff = a - b;
var prod = a * b;
var quot = a / b;
var mod = a % b;

var complex = a + b * 2 - 4 / 2;
var parens = (a + b) * 2;
var nested = ((a + 1) * (b - 1)) / 2;

return sum;
