const greeting = "Hello";
const name = "World";
const message = "Hello, World!";
const empty = "";
const withspaces = "   spaces   ";
const numbers = "12345";

print(greeting);
print(name);
print(message);

const str = "Test";
print(str);

return 0;
