define add(var a, var b) {
    return a + b;
}

define subtract(var a, var b) {
    return a - b;
}

define multiply(var a, var b) {
    return a * b;
}

define noparams() {
    return 42;
}

define noreturn(var x) {
    var temp = x + 1;
    return temp;
}

var x = 10;
var y = 20;
var sum = add(x, y);
var diff = subtract(y, x);
var prod = multiply(sum, diff);
var constval = noparams();

return sum;
