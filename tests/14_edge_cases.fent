const zero = 0;
const one = 1;
const large = 999999;

const dividebyone = 100 / 1;
const modbyone = 100 % 1;

const allops = 1 + 2 - 3 * 4 / 5 % 6;

const deepnest = ((((1 + 2) * 3) - 4) / 5);

if (true) {
    const x = 1;
}

if (false) {
    const y = 2;
} else {
    const z = 3;
}

while (false) {
    const never = 1;
}

define empty() {
    const dummy = 0;
    return dummy;
}

const result = empty();

return zero;
