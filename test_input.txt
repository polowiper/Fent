const x = 42;
const message = "hello world";
const flag = true;

if (x == 10) {
    return x + 5;
} else {
    return x - 3;
}

while (flag) {
    const result = x * 2 / 4 % 3;
    if (result > 5) {
        flag = false;
    }
}

const expr = (10 + 20) * 30;
const check = x < 100;
const negative = !flag;
